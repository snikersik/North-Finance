import SwiftUI

struct SettingsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Settings",
            systemImage: "gearshape",
            message: "North Finance preferences will appear here."
        )
    }
}
