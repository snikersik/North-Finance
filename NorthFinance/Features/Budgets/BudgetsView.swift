import SwiftUI

struct BudgetsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Budgets",
            systemImage: "chart.bar",
            message: "Your category budgets will appear here."
        )
    }
}
