import SwiftUI

struct GoalsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Goals",
            systemImage: "target",
            message: "Your savings goals will appear here."
        )
    }
}
